import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Clicky Game!</h1>
        <p>Click on an image to get a point, click the same image twice and you lose!</p>
    </div>
);

export default Title;

